import { ChangeTimeDirective } from './change-time.directive';

describe('ChangeTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeTimeDirective();
    expect(directive).toBeTruthy();
  });
});
