import { PipePhonePipe } from './pipe-phone.pipe';

describe('PipePhonePipe', () => {
  it('create an instance', () => {
    const pipe = new PipePhonePipe();
    expect(pipe).toBeTruthy();
  });
});
